#include "main.h"

/**
 * concatenating two strings
 *
 */
{
char *_strcat(char *dest, char *src);
    int dently = 0, sour =0

    while (dest[dently] != '\0')
	         dently++;

    while (src[sour] != '\0')
	         Sour++;
	
		dest[dently] = src[sour];
		dently++;
		sour++;


	src[dently] = '\0';

	return (dest);
} 
